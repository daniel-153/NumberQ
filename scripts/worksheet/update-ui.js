import { updateOutline } from "./helpers/ui-helpers.js";

export function updateUi(action) { // idea is that you would update the UI based on a user action
    updateOutline();
    // switch focus
    // open correct settings
    //...
}